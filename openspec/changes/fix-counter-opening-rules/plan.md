# Plan: Fix Counter Opening Rules

Spec ID: `fix-counter-opening-rules`

1. Agregar helpers estrechos en `firestore.rules`.
2. Permitir create/update operativo en `v2_inventory_branch_portions`.
3. Permitir schema legacy real de `v2_inventory_movements`.
4. Permitir create/update operativo en `v2_sucursal_config`.
5. Desplegar reglas con Firebase CLI.
6. Repetir apertura en AVD.

